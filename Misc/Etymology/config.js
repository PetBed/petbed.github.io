/*
  Global Configuration File
  
  Store all your global variables here, especially things
  that might change between development and production,
  like API endpoints.
*/

const config = {
  // The base URL for your study backend API
  API_BASE_URL: 'https://wot-tau.vercel.app/api/study'
};

// Use export to make this available to other modules
export default config;
